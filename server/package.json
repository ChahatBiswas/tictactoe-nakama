{
  "name": "tictactoe-nakama-server",
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --platform=node --target=es2019 --supported:object-extensions=false --outfile=build/index.js",
    "watch": "esbuild src/main.ts --bundle --platform=node --target=es2019 --supported:object-extensions=false --outfile=build/index.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0"
  }
}
